---

apiVersion: v1
kind: Service
metadata:
  name: provisioning-frontend
spec:
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
    name: web
  selector:
    app: provisioning-frontend

---

